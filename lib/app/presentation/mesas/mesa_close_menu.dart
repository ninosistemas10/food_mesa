

import 'package:flutter/material.dart';

class MesaNull extends StatelessWidget {
  const MesaNull({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('hola'),);
  }
}