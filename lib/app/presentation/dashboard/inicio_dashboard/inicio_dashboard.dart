import 'package:flutter/material.dart';


class InicioDashboard extends StatelessWidget {
  const InicioDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('hola Daashboard'),
    );
  }
}