import 'package:flutter/material.dart';

class UsuarioUpdate extends StatelessWidget {
  const UsuarioUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
            color: Colors.green,
        child: const Text('UsuarioUpdate'));
  }
}